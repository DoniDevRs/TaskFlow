import SwiftUI

/// Placeholder root view; replaced by AppCoordinator's hosted navigation (T9).
struct ContentView: View {
    var body: some View {
        Text("TaskFlow")
    }
}

#Preview {
    ContentView()
}
